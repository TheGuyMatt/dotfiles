#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

# LANG=en_US.UTF-8
# LC_MESSAGES=C
# LC_CTYPE=C
# LC_ALL=C

alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '
